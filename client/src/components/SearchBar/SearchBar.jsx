import styles from './searchBar.module.css'

const SearchBar = () => {

   return (
      <div>
         <input type='search'/> 
         <button>Agregar</button>
      </div>
   )
};

export default SearchBar;